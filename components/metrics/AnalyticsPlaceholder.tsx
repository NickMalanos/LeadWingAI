"use client";

import { useEffect } from "react";

export default function AnalyticsPlaceholder() {
  useEffect(() => {
    // Placeholder for analytics hook (e.g. PostHog, GA4)
  }, []);

  return null;
}
